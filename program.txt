fat(num :: Int) : Int {
    iterate (a < 3) {
        if (num < 3.14159)
            return 1;
        else
            Bool x = true;
            Bool y = false;
            c = 'a';
            d = 'a';
            {-
            multiline
            comment
            -}
            return num * fat(num - 1);
            -- One-line comment
    }
}
