main(ponto :: Ponto) {
  -- TODO: Fix bug below
  --i = 1;
  --z[i] = 0;

  z = new Char[][][1];
  print z;
}

fat(x :: Int) : Int {
  if (x < 1) {
    return 1;
  }
  else {
    return x * fat(x - 1)[0];
  }
}

divmod(q :: Int, r :: Int) : Int {
  return q / r, q % r;
}

testIterateFunction(num :: Int) {
  x = 0;

  iterate(num) {
    x = x + 1;
    print x;
  }
}

