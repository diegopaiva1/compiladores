main() {
  if (true)
    print 1 + false;
  else
    print false + 'c';
}

divmod(q :: Int, r :: Int) : Int {
  return q / r, q % r;
}

fat(x :: Int) : Int {
  if (x < 1) {
    return 1;
  }
  else {
    return x * fat(x - 1)[0];
  }
}
