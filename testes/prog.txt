data Ponto {
  x :: Float;
  y :: Float;
}

divmod(q :: Int, r :: Int) : Int {
  return q / r, q % r;
}

testIterateFunction(num :: Int) {
  x = 0;

  iterate(num) {
    x = x + 1;
    print x;
  }
}

main(ponto :: Ponto) {
  -- TODO: Fix bug below
  --i = 1;
  --z[i] = 0;
  z = new Char[];
  print z;

  divmod(3, 2)<x, y>;
  print x;
  print y;

  print fat(5)[0];
}

fat(x :: Int) : Int {
  if (x < 1) {
    return 1;
  }
  else {
    return x * fat(x - 1)[0];
  }
}
