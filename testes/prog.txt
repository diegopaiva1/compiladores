data Ponto {
   x :: Float;
   y :: Float;
}

main() {
  --dummy('a', 1)<x, y, z>;
  --print x.a;
  --x.a = 1.0;
  x = new Ponto;
  x.y = 2.0;
  --x[0] = 1.0;
}

dummy(x :: Char, y :: Int) : Char, Int {
  return x, y;
}

{-
divmod(q :: Int, r :: Int) : Int {
  return q / r, q % r;
}

fat(x :: Int) : Int {
  if (x < 1) {
    return 1;
  }
  else {
    return x * fat(x - 1)[0];
  }
}
-}
